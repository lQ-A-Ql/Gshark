export interface CaptureStatusWireDTO extends Record<string, unknown> {
  file_path?: unknown;
  filePath?: unknown;
  has_capture?: unknown;
  hasCapture?: unknown;
  packet_count?: unknown;
  packetCount?: unknown;
}
