# 开发记录

## 2026-05-26 03:22:28 +08:00 - Codex

- 轮次：Desktop IPC Round 16，MISC `RunMiscModulePackage` typed IPC。
- 改动：新增 `DesktopApp.RunMiscModulePackage(id, values)`，前端 typed override，generic IPC runtime block，Wails binding declaration/wrapper，binding/transport/MISC inventory guardrail更新，WebView smoke 新增 `miscRunBindingAvailable` 断言。
- 验证：`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`、`cd frontend && pnpm run build:wails`、desktop assets、Wails binding、transport policy、generic IPC allowlist、old binding compat、MISC inventory、Wails smoke、`git diff --check` 均通过。
- 证据：`output/desktop-ipc-smoke/desktop-ipc-smoke-summary.json` 中 `desktopRelease.ok = true`，`desktopWebviewTyped.ok = true`，`browserDev.ok = true`，`miscRunBindingAvailable = true`，`directBackendApiRequestCount = 0`。
- 自评：98/100，无硬阻塞；下一轮建议迁移唯一剩余 MISC compatibility route：`ImportMiscModulePackageFromPath` typed IPC，browser-dev multipart HTTP 保留。

## 2026-05-26 03:45:51 +08:00 - Codex

- 轮次：Desktop IPC Round 17，MISC `ImportMiscModulePackageFromPath` typed IPC。
- 改动：新增 `SelectMiscModulePackage()` 原生 ZIP 选择与 `ImportMiscModulePackageFromPath(path)` typed import，DesktopApp 从本地路径读取 zip 并 multipart 转发到现有后端 import route；前端新增桌面原生导入入口，browser-dev multipart HTTP 保留。
- 守护：`POST /api/tools/misc/import` 从 MISC compatibility 移入 migrated typed route，typed binding 存在时 generic IPC 会被 `typed_binding_required` 阻断；MISC inventory 状态更新为 `runtime-complete; list-import-delete-run-typed`。
- 验证：`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`、`cd frontend && pnpm run build:wails`、desktop assets、Wails binding、transport policy、generic IPC allowlist、old binding compat、MISC inventory、Wails smoke、`git diff --check` 均通过。
- 证据：`output/desktop-ipc-smoke/desktop-ipc-smoke-summary.json` 中 `desktopRelease.ok = true`，`desktopWebviewTyped.ok = true`，`browserDev.ok = true`，`miscImportBindingAvailable = true`，`miscDeleteBindingAvailable = true`，`miscRunBindingAvailable = true`，`directBackendApiRequestCount = 0`。
- 自评：97/100，无硬阻塞；下一轮建议进入 generic IPC final compatibility audit，不再新增业务 typed binding。

## 2026-05-26 03:56:47 +08:00 - Codex

- 轮次：Desktop IPC Round 18，generic IPC final compatibility audit。
- 改动：收口 MISC runtime 兼容窗口表述，`check-desktop-misc-compat-inventory.mjs` 改为 MISC transport inventory guardrail，并明确当前没有剩余 MISC desktop compatibility routes；tracker、迁移计划、MISC native binding design 同步更新。
- 守护：MISC modules/import/delete/invoke 均归类为 desktop migrated typed route；browser-dev multipart upload 保留为 HTTP 调试兼容，不再作为 desktop generic IPC 例外描述。
- 验证：focused guardrails 4 文件 / 14 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`、`cd frontend && pnpm run build:wails`、desktop assets、Wails binding、transport policy、generic IPC allowlist、old binding compat、MISC transport inventory、Wails smoke、`git diff --check` 均通过。
- 证据：`output/desktop-ipc-smoke/desktop-ipc-smoke-summary.json` 中 `desktopRelease.ok = true`，`desktopWebviewTyped.ok = true`，`browserDev.ok = true`，`miscImportBindingAvailable = true`，`miscDeleteBindingAvailable = true`，`miscRunBindingAvailable = true`，`directBackendApiRequestCount = 0`。
- 自评：98/100，无硬阻塞；下一轮自动进入 old generated binding exit planning，先定义退出条件，不直接删除兼容 adapter。

## 2026-05-26 04:05:09 +08:00 - Codex

- 轮次：Desktop IPC Round 19，old generated binding exit planning。
- 改动：新增 `docs/desktop-ipc-old-binding-exit-plan.md`，记录旧生成 DesktopApp binding 的允许用途、`InvokeBackendJSON/Blob/Text` 退出触发条件、下线顺序和非目标；`check-desktop-old-binding-compat.mjs` 同时校验代码例外和 exit-plan token。
- 守护：旧 binding 仍只允许 backend bootstrap、HTTP auth token、单一 generic IPC adapter、原生 PCAP/DBC dialog、桌面状态/更新等 shell 方法；不作为新增 data-plane 功能路径。
- 验证：focused old-binding 检查 1 文件 / 4 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`、`cd frontend && pnpm run build:wails`、desktop assets、Wails binding、transport policy、generic IPC allowlist、old binding compat、MISC transport inventory、Wails smoke、`git diff --check` 均通过。
- 证据：`output/desktop-ipc-smoke/desktop-ipc-smoke-summary.json` 中 `desktopRelease.ok = true`，`desktopWebviewTyped.ok = true`，`browserDev.ok = true`，`miscImportBindingAvailable = true`，`miscDeleteBindingAvailable = true`，`miscRunBindingAvailable = true`，`directBackendApiRequestCount = 0`。
- 自评：97/100，无硬阻塞；下一轮自动建议 generic IPC adapter disablement experiment design，先做 flag/分支实验设计，不直接移除生产兼容。

## 2026-05-26 18:49:40 +08:00 - Codex

- 轮次：Desktop IPC Round 20，generic IPC adapter disablement experiment。
- 改动：新增默认关闭的 `VITE_DESKTOP_DISABLE_GENERIC_IPC=1` build-time 实验开关；开启后 `createDesktopBridge` 不创建 `createIpcBackendTransport`，缺 typed data-plane route 直接抛 `generic_ipc_disabled`，不再回退 generic IPC 或 browser HTTP；typed overrides 与 Wails runtime event subscription 保持可用。
- 守护：`scripts/check-desktop-ipc-smoke.ps1` 新增 `-DisableGenericIpcAdapterExperiment`，WebView smoke 结果记录 `genericIpcDisableExperimentRequested` / `genericIpcDisableExperimentBuildFlag`；old-binding exit plan 与 guardrail token 同步验证实验前提。
- 验证：focused tests 3 文件 / 52 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（231 文件 / 736 测试）、`cd frontend && pnpm run build:wails`、desktop assets、Wails binding、transport policy、generic IPC allowlist、old binding compat、MISC inventory、普通 Wails smoke、禁用 adapter 实验 Wails smoke、`git diff --check` 均通过。
- 证据：普通 smoke 中 `genericIpcDisableExperimentBuildFlag = false` 且 `directBackendApiRequestCount = 0`；实验 smoke 使用 `VITE_DESKTOP_DISABLE_GENERIC_IPC=1` + `-DisableGenericIpcAdapterExperiment`，结果 `genericIpcDisableExperimentRequested = true`，`genericIpcDisableExperimentBuildFlag = true`，`desktopWebviewTyped.directBackendApiRequestCount = 0`，`browserDev.ok = true`。实验后已重新执行默认 `build:wails` 恢复 adapter-enabled 资产。
- 自评：99/100，无硬阻塞；下一轮自动建议 generic IPC adapter retirement readiness audit，保持默认 adapter enabled，审计是否可进入生产默认禁用或移除。

## 2026-05-26 19:02:28 +08:00 - Codex

- 轮次：Desktop IPC Round 21，generic IPC adapter retirement readiness audit。
- 改动：新增 `frontend/scripts/check-desktop-generic-ipc-retirement-readiness.mjs` 与对应 Vitest，校验禁用 adapter 实验保持 default-off、源码/烟测/exit-plan token 完整、tracker 记录禁用实验 smoke 事实，并把 `desktop-generic-ipc-retirement:check` 挂入 `pnpm run ci`。
- 守护：CI 会拒绝把 `VITE_DESKTOP_DISABLE_GENERIC_IPC` 改成默认启用、拒绝缺少 `generic_ipc_disabled`/禁用 smoke switch/exit-plan token、拒绝 tracker 缺少 `genericIpcDisableExperimentBuildFlag = true`、`directBackendApiRequestCount = 0`、`browserDevOk = true` 证据。
- 验证：focused tests 5 文件 / 58 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（232 文件 / 740 测试）、`cd frontend && pnpm run build:wails`、desktop assets、Wails binding、transport policy、generic IPC allowlist、retirement readiness、old binding compat、MISC inventory、普通 Wails smoke、禁用 adapter 实验 Wails smoke均通过。
- 证据：普通 smoke 中 `genericIpcDisableExperiment = false`、`genericIpcDisableExperimentBuildFlag = false`、`directBackendApiRequestCount = 0`；实验 smoke 使用 `VITE_DESKTOP_DISABLE_GENERIC_IPC=1` + `-DisableGenericIpcAdapterExperiment`，结果 `genericIpcDisableExperiment = true`、`genericIpcDisableExperimentBuildFlag = true`、`directBackendApiRequestCount = 0`、`browserDev.ok = true`。实验后已重新执行默认 `build:wails` 恢复 adapter-enabled 资产。
- 自评：99/100，无硬阻塞；下一轮自动建议 production-default generic IPC adapter disablement preflight，不删除 adapter 代码，先决定是否把 desktop release 默认策略改为禁用 adapter。

## 2026-05-26 19:26:56 +08:00 - Codex

- 轮次：Desktop IPC Round 22，production-default generic IPC adapter disablement preflight。
- 改动：新增 `desktopGenericIpcPolicy.ts` 策略解析层，默认 `compat`，显式 `VITE_DESKTOP_GENERIC_IPC_POLICY=disabled` 可预检禁用 adapter，legacy `VITE_DESKTOP_DISABLE_GENERIC_IPC=1` 继续可用；`desktopBridge` 改为消费策略解析，WebView smoke 增加 `genericIpcPolicy` 事实字段。
- 守护：retirement readiness guardrail 增加策略模块 token 检查，并拒绝 default-on disabled policy；old-binding exit plan 同步记录 `VITE_DESKTOP_GENERIC_IPC_POLICY=disabled` 预检路径。
- 验证：focused tests 4 文件 / 40 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（233 文件 / 747 测试）、`cd frontend && pnpm run build:wails`、desktop assets、Wails binding、transport policy、generic IPC allowlist、retirement readiness、old binding compat、MISC inventory、普通 Wails smoke、显式 disabled-policy Wails smoke均通过。
- 证据：普通 smoke 中 `genericIpcPolicy = compat`、`genericIpcDisableExperimentBuildFlag = false`、`directBackendApiRequestCount = 0`；disabled-policy smoke 使用 `VITE_DESKTOP_GENERIC_IPC_POLICY=disabled` + `-DisableGenericIpcAdapterExperiment`，结果 `genericIpcPolicy = disabled`、`genericIpcDisableExperimentBuildFlag = true`、`directBackendApiRequestCount = 0`、`browserDev.ok = true`。实验后已重新执行默认 `build:wails` 恢复 adapter-enabled 资产。
- 自评：99/100，无硬阻塞；下一轮自动建议 default-disabled release-candidate rollback guard，不删除 adapter，先补默认禁用候选的回滚/override 证据。

## 2026-05-26 19:39:00 +08:00 - Codex

- 轮次：Desktop IPC Round 23，default-disabled release-candidate rollback guard。
- 改动：新增 `frontend/scripts/check-desktop-generic-ipc-rollback-guard.mjs` 与测试，并挂入 `pnpm run ci`；exit plan 增加 `VITE_DESKTOP_GENERIC_IPC_POLICY=compat` 作为 release-candidate 回滚/override 路径。
- 守护：rollback guard 要求策略模块、策略测试、desktopBridge 测试和 exit plan 同时保留 explicit compat 覆盖 legacy disable alias 的证据，防止后续 default-disabled 决策没有回滚路径。
- 验证：首次 frontend CI 因新增测试文件格式失败，已 Prettier 修复；最终 focused tests 4 文件 / 40 测试通过，`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（234 文件 / 751 测试）、`cd frontend && pnpm run build:wails`、desktop assets、Wails binding、transport policy、generic IPC allowlist、retirement readiness、rollback guard、old binding compat、MISC inventory、普通 Wails smoke均通过。
- 证据：普通 smoke 中 `genericIpcPolicy = compat`、`genericIpcDisableExperimentBuildFlag = false`、`directBackendApiRequestCount = 0`、`browserDev.ok = true`，保留 adapter code 与 browser-dev HTTP/SSE。
- 自评：98/100，无硬阻塞；下一轮自动建议 default-disabled release-candidate decision，保留 `compat` 回滚开关并重跑 normal + disabled-policy smoke。

## 2026-05-26 20:02:10 +08:00 - Codex

- 轮次：Desktop IPC Round 24，default-disabled release-candidate decision。
- 改动：`desktopGenericIpcPolicy.ts` 默认策略切为 `disabled`；显式 `VITE_DESKTOP_GENERIC_IPC_POLICY=compat` 保留为 adapter-enabled rollback；需要 generic IPC/HTTP fallback 的兼容测试均显式设置 `compat`；retirement readiness guardrail 改为验证 default-disabled release candidate 与 tracker rollback 证据。
- 守护：未删除 `InvokeBackendJSON/Blob/Text` 与 `createIpcBackendTransport`；old generated binding 允许清单不变；`generic_ipc_disabled` 错误文案改为策略禁用并提示 `compat` 回滚；browser-dev HTTP/SSE 保持独立可用。
- 验证：focused tests 4 文件 / 42 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（234 文件 / 753 测试）、`cd frontend && pnpm run build:wails`、desktop assets、Wails binding、transport policy、generic IPC allowlist、retirement readiness、rollback guard、old binding compat、MISC inventory、default-disabled Wails smoke、compat rollback Wails smoke均通过。
- 证据：default-disabled smoke 写入 `output/desktop-ipc-smoke/desktop-ipc-smoke-summary.json`，结果 `genericIpcPolicy = disabled`、`genericIpcDisableExperimentBuildFlag = true`、`directBackendApiRequestCount = 0`、`browserDev.ok = true`；compat rollback smoke 写入 `output/desktop-ipc-smoke-compat-rollback/desktop-ipc-smoke-summary.json`，结果 `genericIpcPolicy = compat`、`genericIpcDisableExperimentBuildFlag = false`、`directBackendApiRequestCount = 0`、`browserDev.ok = true`。最终已重新无 env 执行 `build:wails`，dist 保持 default-disabled。
- 自评：99/100，无硬阻塞；下一轮自动建议 default-disabled observation round 1，不删除 adapter code，继续观察默认禁用资产与 `compat` rollback 稳定性。

## 2026-05-26 20:20:14 +08:00 - Codex

- 轮次：Desktop IPC Round 25，default-disabled observation guard。
- 改动：retirement readiness guardrail 新增 `genericIpcAdapterDefaultDisabledReleaseCandidate.observation` 校验，要求三轮 default-disabled 观察后才允许考虑 adapter removal，并拒绝 3/3 前移除 adapter；tracker 记录 observation round 1/3，old-binding exit plan 与 migration plan 同步观察窗口规则。
- 守护：未删除 `InvokeBackendJSON/Blob/Text`、`InvokeBackendBlob`、`InvokeBackendText` 或 `createIpcBackendTransport`；`VITE_DESKTOP_GENERIC_IPC_POLICY=compat` 继续作为 release-candidate rollback；browser-dev HTTP/SSE 保持可用。
- 验证：focused tests 4 文件 / 44 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（234 文件 / 755 测试）、default-disabled `build:wails`、compat rollback `build:wails`、最终 default-disabled `build:wails`、desktop assets、Wails binding、transport policy、generic IPC allowlist、retirement readiness、rollback guard、old binding compat、MISC inventory、`git diff --check` 均通过。
- 证据：default-disabled smoke 写入 `output/desktop-ipc-smoke/desktop-ipc-smoke-summary.json`，结果 `genericIpcPolicy = disabled`、`genericIpcDisableExperimentBuildFlag = true`、`directBackendApiRequestCount = 0`、`browserDev.ok = true`、`capturePackets = 7074`、`httpStreams = 119`、`objectCount = 205`；compat rollback smoke 写入 `output/desktop-ipc-smoke-compat-rollback/desktop-ipc-smoke-summary.json`，结果 `genericIpcPolicy = compat`、`genericIpcDisableExperimentBuildFlag = false`、`directBackendApiRequestCount = 0`、`browserDev.ok = true`。
- 自评：99/100，无硬阻塞；下一轮自动进入 default-disabled observation round 2，继续不删除 adapter code，仅累计第二轮 default-disabled 与 compat rollback 证据。

## 2026-05-26 20:30:52 +08:00 - Codex

- 轮次：Desktop IPC Round 26，default-disabled observation round 2。
- 改动：retirement readiness guardrail 从只校验 latest observation round 改为逐个校验 counted observation rounds；每个 counted round 都必须记录 default-disabled policy、compat rollback、`directBackendApiRequestCount = 0`、`browserDevOk = true`、`adapterRemoved = false`。tracker 累计 observation 2/3。
- 守护：未删除 `InvokeBackendJSON/Blob/Text` 与 `createIpcBackendTransport`；`VITE_DESKTOP_GENERIC_IPC_POLICY=compat` 继续作为 release-candidate rollback；browser-dev HTTP/SSE 保持可用。
- 验证：focused tests 4 文件 / 45 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（234 文件 / 756 测试）、default-disabled `build:wails`、compat rollback `build:wails`、最终 default-disabled `build:wails` 均通过。
- 证据：default-disabled smoke 写入 `output/desktop-ipc-smoke/desktop-ipc-smoke-summary.json`，结果 `genericIpcPolicy = disabled`、`genericIpcDisableExperimentBuildFlag = true`、`directBackendApiRequestCount = 0`、`browserDev.ok = true`、`capturePackets = 7074`、`httpStreams = 119`、`objectCount = 205`；compat rollback smoke 写入 `output/desktop-ipc-smoke-compat-rollback/desktop-ipc-smoke-summary.json`，结果 `genericIpcPolicy = compat`、`genericIpcDisableExperimentBuildFlag = false`、`directBackendApiRequestCount = 0`、`browserDev.ok = true`。
- 自评：99/100，无硬阻塞；下一轮自动进入 default-disabled observation round 3。即使第三轮通过，也只能进入单独 adapter-removal preflight round，不能在 observation round 3 同轮删除 adapter。

## 2026-05-26 21:09:38 +08:00 - Codex

- 轮次：Desktop IPC Round 27，default-disabled observation round 3。
- 改动：tracker 累计 observation 3/3，`genericIpcAdapterDefaultDisabledReleaseCandidate.status = observation-round-3-completed`，`removalAllowed = true` 仅作为进入单独 preflight 的调度信号；migration plan 追加 Round 27，并将下一轮建议切换为 generic IPC adapter removal preflight。
- 守护：本轮未删除 `InvokeBackendJSON/Blob/Text` 与 `createIpcBackendTransport`；`VITE_DESKTOP_GENERIC_IPC_POLICY=compat` 继续作为 release-candidate rollback；browser-dev HTTP/SSE 保持可用。
- 验证：focused tests 4 文件 / 45 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（234 文件 / 756 测试）、default-disabled `build:wails`、compat rollback `build:wails`、最终 default-disabled `build:wails` 均通过。
- 证据：default-disabled smoke 写入 `output/desktop-ipc-smoke/desktop-ipc-smoke-summary.json`，结果 `genericIpcPolicy = disabled`、`genericIpcDisableExperimentBuildFlag = true`、`directBackendApiRequestCount = 0`、`browserDev.ok = true`、`capturePackets = 7074`、`httpStreams = 119`、`objectCount = 205`；compat rollback smoke 写入 `output/desktop-ipc-smoke-compat-rollback/desktop-ipc-smoke-summary.json`，结果 `genericIpcPolicy = compat`、`genericIpcDisableExperimentBuildFlag = false`、`directBackendApiRequestCount = 0`、`browserDev.ok = true`。
- 自评：99/100，无硬阻塞；下一轮自动进入 generic IPC adapter removal preflight。preflight 先 inventory 引用、验证 browser-dev/rollback 解耦和可逆删除计划，发现 blocker 则停留 governance，不删除 adapter。

## 2026-05-26 21:18:43 +08:00 - Codex

- 轮次：Desktop IPC Round 28，generic IPC adapter removal preflight。
- 改动：新增 `frontend/scripts/check-desktop-generic-ipc-removal-preflight.mjs` 与测试，并接入 `pnpm run ci`；tracker 新增 `genericIpcAdapterRemovalPreflight`，固定 adapter 引用清单、rollback 要求、browser-dev HTTP/SSE 要求和可逆删除计划；migration plan 与 old-binding exit plan 同步 preflight 规则。
- 守护：本轮未删除 `InvokeBackendJSON/Blob/Text` 与 `createIpcBackendTransport`；preflight guardrail 会拒绝新的 adapter token 扩散，且要求 `adapterRemoved = false`。
- 验证：focused tests 5 文件 / 51 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（235 文件 / 762 测试）、`cd frontend && pnpm run build:wails` 均通过。
- 证据：生产源码 adapter 引用只允许出现在 `desktopBridge.ts`、`desktopTransportBindingShell.ts`、`ipcBackendTransport.ts`；generated binding 引用只允许出现在 `frontend/wailsjs/go/main/DesktopApp.d.ts` 与 `DesktopApp.js`。Round 27 的 3/3 observation smoke 继续作为 candidate removal 的前置证据。
- 自评：98/100，无硬阻塞；下一轮自动进入 generic IPC adapter removal candidate。候选删除轮必须重新跑 default-disabled Wails smoke；任一 gate 失败则恢复 adapter，停留 governance。

## 2026-05-26 21:41:45 +08:00 - Codex

- 轮次：Desktop IPC Round 29，generic IPC adapter removal candidate。
- 改动：`desktopBridge` 不再 import/construct `createIpcBackendTransport`，桌面 data-plane base 固定为 `createDisabledGenericIpcBackendTransport`；typed overrides 继续优先；缺 typed data-plane binding 直接 `generic_ipc_disabled`；runtime/MCP/capture/TLS 控制面兼容 fallback 显式走 `fallbackBridge`；Wails runtime event subscription 继续通过 `EventsOn`。
- 策略：`VITE_DESKTOP_GENERIC_IPC_POLICY=compat` 保留为可识别策略值，但不再恢复 generic IPC adapter，是 documented no-op。`ipcBackendTransport.ts`、`desktopTransportBindingShell.ts`、generated Wails `InvokeBackendJSON/Blob/Text` 仍作为下一轮 cleanup preflight 的 inventory，不在本轮删除。
- 守护：retirement readiness、rollback guard、removal preflight guardrail 同步为 compat no-op contract；tracker 标记 `frontendAdapterConstructionRemoved = true`，但 `adapterRemoved = false`。
- 验证：focused tests 7 文件 / 82 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（235 文件 / 766 测试）、`cd frontend && pnpm run build:wails`、default-disabled Wails smoke、Wails binding、transport policy、generic IPC allowlist、retirement readiness、rollback/no-op guard、removal preflight、old-binding compat、MISC inventory、desktop assets、`git diff --check` 均通过。
- 证据：default-disabled Wails smoke 写入 `output/desktop-ipc-smoke/desktop-ipc-smoke-summary.json`，结果 `genericIpcPolicy = disabled`、`genericIpcDisableExperimentBuildFlag = true`、`directBackendApiRequestCount = 0`、`browserDev.ok = true`、`capturePackets = 7074`、`httpStreams = 119`、`objectCount = 205`。
- 自评：97/100，无硬阻塞；下一轮自动进入 backend/generated generic IPC binding cleanup preflight。下一轮仍只 inventory 与设计 guardrail，不删除 backend/generated bindings。

## 2026-05-26 22:29:38 +08:00 - Codex

- 轮次：Desktop IPC Round 30，backend/generated generic IPC binding cleanup preflight。
- 改动：新增 `frontend/scripts/check-desktop-generic-ipc-binding-cleanup-preflight.mjs` 与测试，并接入 `pnpm run ci`；tracker 新增 `genericIpcBackendGeneratedBindingCleanupPreflight`；migration plan 与 old-binding exit plan 同步 backend/generated cleanup preflight contract。
- 事实：当前 `deletionReady = false`。后端仍导出 `DesktopApp.InvokeBackendJSON/Blob/Text`；`DownloadObjectsZip`、`GetWinRMDecryptResultText`、`ExportWinRMDecryptResult`、`ExportMediaBatchTranscription`、`DownloadMediaArtifact`、`GetMediaPlaybackBlob` 仍复用 `InvokeBackendBlob/Text`；`check-wails-bindings` 仍要求 `generic-ipc` group；`ipcBackendTransport.ts` 仍包含 unreachable adapter implementation 和 active disabled transport。
- 验证：focused Vitest 6 文件 / 73 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（236 文件 / 772 测试）、`cd frontend && pnpm run build:wails`、Wails binding、transport policy、removal preflight、binding cleanup preflight、old-binding compat、`git diff --check` 均通过。
- 自评：92/100，无硬阻塞；本轮不重复 Wails smoke，因为只新增 inventory guardrail 与文档，不改 runtime transport。
- 下一轮自动建议：typed blob/text helper split。先把 typed backend methods 从 exported `InvokeBackendBlob/Text` 调用迁到 non-exported helpers，并更新 `desktop_backend_proxy_test.go`，再考虑 generated binding deletion。

## 2026-05-26 22:41:52 +08:00 - Codex

- 轮次：Desktop IPC Round 31，typed blob/text helper split。
- 改动：`desktop_backend_proxy.go` 新增 non-exported `invokeBackendBlob` / `invokeBackendText`，`InvokeBackendBlob` / `InvokeBackendText` 仅保留为 generated binding 兼容转发；`DownloadObjectsZip`、WinRM text/export、media batch export、media artifact download、media playback 改为调用 lowercase helper。
- 测试：`desktop_backend_proxy_test.go` 将 blob/text 语义覆盖改到 typed methods；`check-desktop-generic-ipc-binding-cleanup-preflight` 与测试更新为 `typedHelperReuse = []` 的当前事实，并保留旧 reuse 回归检测。
- 验证：`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（236 文件 / 773 测试）、`cd frontend && pnpm run build:wails`、Wails binding、binding cleanup preflight、removal preflight、old-binding compat、JSON parse、`git diff --check` 均通过。
- 自评：95/100，无硬阻塞；本轮不重复 Wails smoke，因为没有改 WebView transport 或 generated binding 表面。
- 下一轮自动建议：disabled transport module split。把 `createDisabledGenericIpcBackendTransport` 和 `generic_ipc_disabled` fail-fast 行为从 `ipcBackendTransport.ts` 移到独立模块，保留 legacy adapter inventory，暂不删除 generated `InvokeBackend*`。

## 2026-05-26 22:54:29 +08:00 - Codex

- 轮次：Desktop IPC Round 32，disabled transport module split。
- 改动：新增 `desktopDisabledGenericIpcTransport.ts` 承接 active disabled generic IPC transport 和 `generic_ipc_disabled` fail-fast 行为；新增 `desktopEventTransport.ts` 复用 Wails runtime events；`ipcBackendTransport.ts` 只保留 unreachable generic adapter inventory 与 shared IPC controls；`desktopBridge.ts` 改为从新模块引入 disabled transport。
- 守护：`check-desktop-generic-ipc-binding-cleanup-preflight` 与 `check-desktop-generic-ipc-retirement-readiness` 已更新为 split 后事实，cleanup blockers 删除 “createDisabledGenericIpcBackendTransport must be moved” 一项；backend/generated `InvokeBackendJSON/Blob/Text` 未删除。
- 验证：focused tests 6 文件 / 76 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（237 文件 / 773 测试）、`cd frontend && pnpm run build:wails`、desktop assets、Wails binding、binding cleanup preflight、retirement readiness、removal preflight、old-binding compat、JSON parse、`git diff --check` 均通过。一次并行 standalone desktop asset check 在 build 产物写入前失败，顺序重跑通过，判定为命令竞态。
- 自评：96/100，无硬阻塞；本轮未跑真实 Wails smoke，因为 runtime data-plane policy 未改变。
- 下一轮自动建议：backend/generated `InvokeBackendJSON` deletion preflight。先处理 `desktop_backend_proxy_test.go -> InvokeBackendJSON` direct test blocker，再评估是否进入 generated binding deletion round。

## 2026-05-27 00:30:01 +08:00 - Codex

- 轮次：Desktop IPC Round 33，InvokeBackendJSON direct test preflight。
- 改动：`desktop_backend_proxy_test.go` 不再直接调用 exported `DesktopApp.InvokeBackendJSON`；JSON proxy 行为改由 typed `GetIndustrialAnalysis` 覆盖，multipart proxy 行为改由 typed `ImportMiscModulePackageFromPath` 覆盖。
- 守护：`desktop-ipc-iteration-status.json`、migration plan、old-binding exit plan 已同步；binding cleanup preflight 的 direct-test blocker 已消失，`deletionReady` 仍为 false，因为 generated Wails bindings、`check-wails-bindings generic-ipc group` 和 `ipcBackendTransport.ts` legacy adapter inventory 仍在。
- 验证：`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（237 文件 / 773 测试）、`cd frontend && pnpm run build:wails`、Wails binding、binding cleanup preflight、removal preflight、old-binding compat、JSON parse、`git diff --check` 均通过。
- 自评：95/100，无硬阻塞；本轮未跑真实 Wails smoke，因为 runtime data-plane 行为未改变。
- 下一轮自动建议：backend/generated `InvokeBackend*` deletion candidate。下一轮必须同轮处理 backend methods、generated Wails bindings、`desktopTransportBindingShell`、legacy adapter inventory、`check-wails-bindings generic-ipc group` 和 old-binding compat allowlist。

## 2026-05-27 00:48:49 +08:00 - Codex

- 轮次：Desktop IPC Round 34，backend/generated `InvokeBackend*` deletion candidate。
- 改动：删除 backend exported `DesktopApp.InvokeBackendJSON` / `InvokeBackendBlob` / `InvokeBackendText`；删除 generated Wails `InvokeBackendJSON` / `InvokeBackendBlob` / `InvokeBackendText`；删除 `desktopTransportBindingShell.ts` 中旧字段；删除 `ipcBackendTransport.ts` legacy adapter inventory。
- 守护：新增 `desktopIpcControls.ts` 承接 `DesktopIpcRequestError`、`IpcBackendTransport`、`withDesktopIpcControls` 和 50MB blob 上限；`desktopDisabledGenericIpcTransport.ts` 继续作为 `generic_ipc_disabled` fail-fast transport；`check-wails-bindings` 改为禁止 removed `InvokeBackend*`；old-binding compat 不再允许 `ipcBackendTransport.ts` 旧库存。
- 验证：focused Vitest 7 文件 / 65 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（237 文件 / 759 测试）、`cd frontend && pnpm run build:wails`、Wails binding、binding cleanup preflight、removal preflight、retirement readiness、old-binding compat、`git diff --check` 均通过。
- Smoke：`scripts/check-desktop-ipc-smoke.ps1` 通过；WebView typed `directBackendApiRequestCount = 0`、`totalInstrumentedNetworkRequests = 0`、`genericIpcPolicy = disabled`、`genericIpcDisableExperimentBuildFlag = true`；browser-dev `ok = true`。
- 自评：98/100，无硬阻塞；backend/generated `InvokeBackend*` deletion 和 post-removal audit 完成。
- 下一轮自动建议：generic IPC post-removal monitoring 或 shell API typed cleanup；不得恢复 generic IPC data-plane，browser-dev HTTP/SSE 保持。

## 2026-05-27 01:24:10 +08:00 - Codex

- 轮次：Desktop IPC Round 35，generic IPC post-removal monitoring。
- 改动：新增 `frontend/scripts/check-desktop-generic-ipc-post-removal-monitor.mjs` 与测试，并接入 `pnpm run ci` 为 `desktop-generic-ipc-post-removal:check`；README 桌面 IPC 段落移除过期 `InvokeBackendJSON/Blob/Text` 描述，改为 typed IPC first、`generic_ipc_disabled` fail-fast、browser-dev HTTP/SSE 保留、Wails runtime events 保留。
- 守护：monitor 会拒绝恢复 `ipcBackendTransport.ts`、`InvokeBackendJSON` / `InvokeBackendBlob` / `InvokeBackendText`、`createIpcBackendTransport`、README 旧措辞、tracker post-removal audit 证据漂移和 CI 未接入。
- 验证：focused Vitest 4 文件 / 30 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（238 文件 / 766 测试）、`cd frontend && pnpm run build:wails`、post-removal monitor、removal preflight、binding cleanup preflight、Wails binding、`git diff --check` 均通过。
- Smoke：本轮不改 runtime transport，未重复 Wails smoke；沿用 Round 34 post-removal smoke 证据，`directBackendApiRequestCount = 0`、`totalInstrumentedNetworkRequests = 0`、`genericIpcPolicy = disabled`、`genericIpcDisableExperimentBuildFlag = true`、browser-dev green。
- 自评：97/100，无硬阻塞；下一轮自动建议 final closure audit，或先做一轮 shell compatibility wording cleanup。不得恢复 generic IPC data-plane。

## 2026-05-27 01:35:42 +08:00 - Codex

- 轮次：Desktop IPC Round 36，final closure audit。
- 改动：本轮不改数据面运行时代码；更新 `desktop-ipc-iteration-status.json` 为 `final-closure-completed`，同步 migration plan、old-binding exit plan，并新增 final closure audit 归档报告。
- 策略：desktop release 保持 typed IPC first；frontend generic IPC adapter construction 已移除；backend/generated `InvokeBackendJSON` / `InvokeBackendBlob` / `InvokeBackendText` 已移除；缺已迁移数据面 typed coverage 继续 `generic_ipc_disabled` fail-fast；browser-dev HTTP/SSE 保留。
- 验证：focused Vitest 7 文件 / 63 测试通过；`go test -tags dev ./...`、`go test -tags production ./...`、`cd backend && go test ./...`、`cd frontend && pnpm run ci`（238 文件 / 766 测试）、`cd frontend && pnpm run build:wails`、post-removal monitor、removal preflight、binding cleanup preflight、old-binding compat、Wails binding 均通过。
- Smoke：`scripts/check-desktop-ipc-smoke.ps1` 通过；desktop release、WebView typed、browser-dev 均 green；WebView typed `directBackendApiRequestCount = 0`、`totalInstrumentedNetworkRequests = 0`、`genericIpcPolicy = disabled`、`genericIpcDisableExperimentBuildFlag = true`、`capturePackets = 7074`、`httpStreams = 119`、`objectCount = 205`。
- 自评：99/100，无硬阻塞；迁移进入维护态。下一轮只做显式 scoped maintenance，不恢复 generic IPC data-plane。

## 2026-05-28 全量代码审计 + 流量图增强 + 插件管理 UI

- 轮次：全量代码审计 5 轮 + 功能开发 2 项。
- 范围：后端 engine/transport/tshark/plugin/model 全量；前端 features/pages/mappers/wire/types 全量。
- 审计发现并修复 37 项缺陷：
  - **P0 死锁**: `RawStream`/`RawStreamPage` 持有 `s.mu.RLock()` 时调用 `streamWithOverrides` 导致可重入死锁，改为释放后调用。
  - **P0 插件运行时**: `isLogicFile()` 接受 `.ts/.lua/.go` 但运行时不支持，改为只接受 `.js/.mjs/.cjs/.py`。
  - **P1 媒体刷新**: `useMediaAnalysis` 硬编码 `getMediaAnalysis(false, ...)` 导致刷新按钮不生效，改为传递 `force` 参数。
  - **P1 panic recovery**: transport 层无 `recover()` 中间件，新增 `withRecovery`。
  - **P1 错误码**: `handlePacket*` 全部返回 500，改为区分 400/404/500。
  - **P1 method guard**: 16+ 只读 handler 无 HTTP method 检查，新增 `requireMethod` helper。
  - **P1 路径遍历**: `handleCaptureStart`/`handleVehicleDBC` 无路径遍历检查。
  - **P1 TOCTOU**: `cachedAnalysis` 写入前未检查 pcap 是否变更。
  - **P1 USB 字段**: `usbMassStorageMapper` 缺少 3 个字段映射。
  - **P2 YARA 锁重构**: `cachedYaraHitsWithContext` 持有 `yaraMu.Lock` 整个扫描周期（最长 25s），改为锁外执行扫描、原子写入结果。
  - **P2 工控扫描器**: 单个协议扫描器失败中止整个分析，改为累积错误返回部分结果。
  - **P2 DBC 解析**: `strconv` 错误静默丢弃，改为记录日志。
  - **P2 union 校验**: 4 个 mapper 使用 unsafe cast，改为运行时校验回退默认值。
  - **P2 证据收集**: `GatherEvidence` 缺少 `media` 和 `misc` 模块，新增对应 collector。
  - **P2 tshark 超时**: `scanFieldRows` 使用 `Command()` 无超时，改为 `CommandContext` 30s 超时。
  - **P2 前端 hook 错误处理**: 3 个 hook 静默吞掉错误，改为设置 error 状态。
- 流量图增强：
  - 新增 `TrafficAreaChart`：Catmull-Rom 平滑 SVG 面积图，超过 200 点自动降采样。
  - 新增 `TrafficTopologyGraph`：源→目标节点 + 曲线边 + 箭头，自环过滤，安全 Math.max。
  - 新增 `TrafficProtocolTree`：从 `frame.protocols` 构建可展开/折叠树，迭代式 flatten，maxDepth 执行。
  - 后端新增 `frame.protocols` 字段采集 + `ProtocolTreeNode` 类型 + `buildProtocolTree`。
- 插件管理 UI：
  - 新增 `/plugin-manager` 路由 + 侧边栏导航。
  - 新增 `usePluginManager` hook：unmount 守卫（mountedRef）、并发操作排序（withOp）、统一 re-throw。
  - 新增 `PluginManagerShell`：表格 + 工具栏 + 添加对话框，ARIA 属性（role=checkbox/grid, aria-label），React.memo PluginRow。
  - 新增 `PluginSourceEditor`：Escape/遮罩关闭、source prop 同步、dirty 确认、focus trap、aria-hidden。
- 验证：`cd backend && go test ./...` 全部通过；`cd frontend && pnpm run typecheck` 通过；`cd frontend && pnpm run test:run` 240 文件 / 778 测试全部通过；`gofmt -l .` 无格式问题。
- 改动：58 文件，+2131/-135 行。
- 自评：95/100；剩余 LOW 项（Packets 全量返回、parseInt 日志、ffmpeg 超时已由 CommandContext 处理、DBC 路径由 transport 层防护、parseFlexibleKey 为 CTF 设计、probe 锁使用不同互斥量）均为合理设计选择，无需修复。

## 2026-05-28 插件模块全量删除

- 轮次：插件清理 Round 1，整个 plugin 模块（后端 + 前端 + 桌面代理 + CI 脚本 + 文档）一次性删除。
- 前置操作：将非插件功能从 plugin 文件中拆分至独立文件，保留车辆 DBC 和 TLS 解密能力：
  - `dbcMapper.ts`：`asDBCProfile` / `asDBCProfiles` 从 `pluginMapper.ts` 拆出。
  - `vehicleWireDtos.ts`：新增 `DBCProfileWireDTO`、`DecryptionConfigWireDTO`，从 `pluginWireDtos.ts` 拆出。
  - `toolClient.ts`：车辆 DBC（list/add/remove）和 TLS（configure/get）方法从 `pluginClient.ts` 迁入。
- 后端删除：
  - `internal/plugin/` 整包（manager.go、runtime.go、manager_test.go）。
  - `service.go`：`NewService` 签名从 `(emitter, pm)` 改为 `(emitter)`，移除 `pluginManager` 字段。
  - `service_tools.go`：移除 8 个 plugin proxy 方法 + `ThreatHunt` 中 plugin runner 段落。
  - `transport/services.go`：移除 `PluginService` 接口。
  - `transport/http_server.go`：移除 `plugins` 字段、6 条路由、6 个 handler、审计映射。
  - `mcp/server.go`：移除 `Plugins` 字段和接口断言。
  - `cmd/sentinel/main.go`：移除 plugin manager 初始化。
  - `desktop_backend_proxy.go`：移除 3 个请求结构体 + 7 个 Wails proxy 方法。
  - 19 个 engine 测试文件：`NewService(nil, nil)` → `NewService(nil)`。
  - transport 测试：移除 `mcpContractPluginService`、`fakePluginService`、plugin route 测试。
- 前端删除：
  - 8 个文件：`pluginClient.ts`、`pluginMapper.ts`、`pluginSourceMapper.ts`、`pluginWireDtos.ts`、`desktopTypedBridgePlugin.ts`、`ipcBackendTransport.ts`，以及各自测试。
  - `bridgeTypes.ts`：移除 `PluginClient`、`PluginSource` 类型。
  - `backendBridgeTransport.ts`：移除 plugin client 创建；DBC/TLS 接线迁至 `toolClient`。
  - `desktopTypedBridge.ts`：移除 plugin typed overrides。
  - `desktopTypedBridgeRequirements.ts`：移除 7 条 plugin binding。
  - `desktopTransportBindingTooling.ts`：移除 7 个 plugin 类型。
  - `core/types/traffic.ts`：移除 `PluginItem` 接口。
  - `wailsBridge.ts`：移除 plugin re-exports。
  - `bridgeDomains.ts`：移除 plugin client projection。
  - `bridgeDomains.test.ts`、`httpBridgeAggregation.test.ts`、`contractMapper.property.test.ts`、`desktopBridge.test.ts`：更新断言。
- CI / 文档：
  - 6 个 CI 脚本：移除 plugin 绑定/类型/映射/引用。
  - plugin interface 文档：删除。
  - `docs/governance-defect-register.json`：移除 P0-4 条目。
- 验证：`cd backend && go test ./...`（7 packages）通过；`cd frontend && pnpm run typecheck` 通过；`cd frontend && pnpm run test:run`（235 files / 757 tests）通过；`gofmt -w .` 格式化后 `gofmt -l .` 无输出。
- 改动：110 files changed, +4622/-5298 行。
- commit：`12615c78`，`refactor: remove entire plugin module (backend + frontend + desktop)`。
- 自评：97/100；DBC/TLS 功能完整保留，无功能回归。下一轮可进入流量分析功能增强或新模块开发。

## 2026-05-28 全量审计报告

### 项目架构
- **后端**: Go (1.22/1.25) + Wails，分层架构 (engine/tshark/transport)，标准库路由，组件化模块 (工业/车辆/媒体/C2/APT/USB)。
- **前端**: React + Vite + TypeScript，组件化设计，Zustand/Context 状态管理，Hooks 驱动业务逻辑。

### 算法设计
- **C2 解密**: 支持 VShell (MD5 三重 KDF) 和 CS (AES-CBC/GCM)，实现了候选流自动提取和多密钥尝试。
- **威胁狩猎**: 包含 CTF Flag 检测 (明文/Base64/Hex)、非标端口识别和异常 404/403 爆破检测。
- **流量分析**: 基于 TShark 的深度协议解析，支持流重组、对象提取和媒体还原。

### 功能实现
- 完整的数据包捕获、过滤、分页和流追踪功能。
- 多模态证据聚合 (Evidence Aggregation) 覆盖 9 大分析模块。
- 媒体流自动识别、裸流导出及基于 FFmpeg 的转码播放。
- 桌面端 IPC 机制完善，支持原生文件对话框和后台任务管理。

### 缺陷与风险 (P2)
1. **C2 算法强度**: VShell 依赖 MD5 (已不推荐用于新协议)，且解密仅作参考，需人工复核。
2. **前端错误处理**: 部分 Hooks 存在静默吞错风险，建议统一 Error Boundary。
3. **治理模块**: ShouldTriggerSelfAudit 仅在特定轮次触发，可能存在审计盲区。

### 安全加固建议
- 增加对上传文件/路径的严格白名单校验。
- 后端对外部命令 (tshark/ffmpeg) 执行增加更严格的超时和资源限制。

_审计人: Codex | 时间: 2026-05-28_

## 2026-06-06 17:51:08 +08:00 - Codex

- 轮次：preload hot-loading plan 审计；本轮只读代码与文档，不改运行时代码。
- 已读新增文档：`docs/preload-hot-loading-plan.md`、`docs/README.md` 最新入口更新、`docs/开发记录.md` 最近开发日志，并核对 README / CLAUDE / ignore 现有未提交改动。
- 代码核对：
  - 前端 `routes.tsx` 已集中使用 React Router lazy imports，`MainSidebarNav` + `NAV_ITEMS` 可承接 hover/focus route chunk preload。
  - Traffic / Evidence / C2 / Industrial / Vehicle / USB hooks 已有 capture-key cache + abortable request，但多数无 module-level inflight/prefetch contract，Phase 2 需要先抽共享 cache/inflight API，不能从 scheduler 直接调用 hook。
  - HTTP handlers 已按当前规则传入 `r.Context()`；backend engine 已有部分 `analysisInFlightGroup` singleflight 与 stale capture guard。
  - Heavy analysis 仍缺 backend-wide limiter / priority queue；部分 cold path 仍用 `context.Background()`（如 C2 packet builder、media non-force singleflight、tshark field cache warmer），Phase 3 不能直接接 warmup。
- 文档评审结论：计划总体合理，Phase 1 可直接落地；Phase 2 基本合理但需把 Evidence 预取限定为 metadata/first-page/summary，避免实际 `/api/evidence` 聚合触发 C2/APT/Media/Object 等重分析；Phase 3 安全门槛写得对，但必须先独立完成 backend limiter、source 标记、timeout/cancel 和 priority tests。
- 建议调整：
  - Phase 0 增加 target inventory 与现有 hook/cache contract gap 表。
  - Phase 1 增加 `rules` route 覆盖，且 preload registry 与 route lazy loader 必须共源，避免 import registry 漂移。
  - Phase 2 明确“不通过 React hook 触发预取”，只通过 feature preload contract/domain client；Evidence 默认只预取轻量摘要，不默认 GatherEvidence 全模块。
  - Phase 3 增加 hard gate：没有 backend limiter + context propagation 证据时，heavy warmup 只能停留 design/preflight。
- 风险评级：Phase 1 低；Phase 2 中；Phase 3 高。综合评分：86/100，可进入 Phase 0/1；不建议提前实施 heavy warmup。

_审计人: Codex | 时间: 2026-06-06 17:51:08 +08:00_

## 2026-05-28 品牌迁移 gshark → meow_traffic

- 轮次：品牌全量迁移 Round 1。
- 改动：将项目中所有用户可见品牌标识从 gshark 迁移为 meow_traffic/meow-traffic/meow~traffic。
  - CSS 变量：--gshark-* → --meow-*（theme.css + 152 前端文件，~371 处）
  - 环境变量：GSHARK_* → MEOW_TRAFFIC_*（~20 Go/PS1/MJS 文件）
  - 目录名：gshark-sentinel → meow-traffic（配置/临时/插件目录）
  - API identity："gshark-sentinel" → "meow-traffic"（运行时身份标识）
  - Wails 事件名：gshark:backend:* → meow:backend:*（桌面 IPC 事件）
  - 自定义事件：gshark:focus-filter → meow:focus-filter
  - 文档：AGENTS.md、CLAUDE.md、README.md、docs/*.md 全部更新
- 不变项：Go module path (github.com/gshark/sentinel/...)、后端二进制名 (sentinel-backend.exe)、历史审计报告。
- 验证：cd backend && go test ./...（8 packages）通过；cd frontend && pnpm run typecheck 通过；cd frontend && pnpm run test:run（235 files / 757 tests）通过；cd frontend && pnpm run lint 通过；cd frontend && pnpm run build 通过；gofmt -l . 无输出。
- 自评：96/100；Go module path 和 sentinel-backend 二进制名按约定保留。

_审计人: Codex | 时间: 2026-05-28_

## 2026-06-06 19:20:14 +08:00 - Codex

- 轮次：preload hot-loading plan 全阶段实施。
- 文档输入：已复读 `docs/preload-hot-loading-plan.md`、`docs/README.md`、`docs/开发记录.md`，并按 AGENTS 要求在本轮末尾续写记录。
- Phase 0/1：
  - 新增 `frontend/src/app/preload/`：target inventory、budget、telemetry、route preload、scheduler、metrics、flags。
  - `routes.tsx` 抽为 `routeModuleLoaders.ts` 共源 lazy registry；`MainSidebarNav` hover/focus code preload；`MainLayout` idle neighbor preload。
  - 新增 `check-preload-budget.mjs` / `check-preload-boundaries.mjs` 并接入 frontend CI。
- Phase 2：
  - Traffic/Evidence 抽 cache/inflight/preload contract；页面 mount 可复用 preload 结果。
  - Evidence background preload 强制 `modules=["hunting"]`，禁止 all-module `/api/evidence` 预取。
  - Hunting 只预取 runtime config；Rules direct fetch 迁入 domain client，只预取 rules status；补 typed desktop rules binding。
- Phase 3：
  - 后端新增 `AnalysisRequestMeta`、warmup limiter、context-aware specialized field warmer、C2 cold ctx 传递、warmup/user singleflight fallback。
  - C2/Industrial/Vehicle/USB 接入 warmup meta；HTTP 仅复用现有 route query `warmup=1`，未新增公开 route。
  - 前端 heavy gate 默认 off，需 desktop + flag + capture-ready + route idle + hover 400ms；Vehicle key 含 DBC paths，USB key 含 HID source/limit；APT/Media/Object/MISC 保持禁止。
- Phase 4：
  - 前端新增 metrics summary/downgrade、target disable、code-only rollback。
  - 后端新增 internal warmup/request telemetry 聚合与 status event：`__analysis_warmup__`、`__analysis_request__`，记录 queue depth、waitMs、durationMs、status。
- 验证：
  - `cd backend && gofmt -l .`：通过。
  - `cd backend && go test ./...`：通过。
  - `cd frontend && pnpm run typecheck`：通过。
  - `cd frontend && pnpm run lint`：通过。
  - `cd frontend && pnpm run format:check`：通过。
  - `cd frontend && pnpm run preload:budget:check && pnpm run preload:boundary:check && pnpm run boundary:check && pnpm run wails-binding:check`：通过。
  - `cd frontend && pnpm run test:run`：通过，262 files / 883 tests。
  - `cd frontend && pnpm run build`：通过。
  - `cd frontend && pnpm run size:check`：失败，但只包含既有 size-budget baseline 文件，未包含本轮新增 preload/layout 文件。
- 自评：93/100。剩余项：前端 size-budget 旧债需单独拆分治理；后端 telemetry 当前为内存聚合 + status event，未做 metrics UI。

_实施人: Codex | 时间: 2026-06-06 19:20:14 +08:00_

## 2026-06-06 19:40:32 +08:00 - Codex

- 轮次：preload hot-loading 代码健壮性复审、提交前验证、size-budget 拆分方案制定。
- 复审发现：
  - 后端新增 internal telemetry status event（`__analysis_warmup__`、`__analysis_request__`，并兼容既有 `__evidence_timing__`）可能被前端 lifecycle 当作普通 backend status 展示。
  - 该问题不影响请求正确性，但会污染用户可见状态栏，属于 P2 健壮性/UX 问题。
- 修复：
  - `frontend/src/app/state/backendStatusMessage.ts` 新增 `isInternalTelemetryStatusMessage`。
  - `frontend/src/app/state/hooks/backendLifecycleEvents.ts` 在 status handler 中丢弃 internal telemetry，不触发 progress、capture waiters 或状态栏更新。
  - `backendStatusMessage.test.ts`、`useBackendLifecycle.test.tsx` 补覆盖。
- 验证：
  - `cd frontend && pnpm exec vitest run src/app/state/backendStatusMessage.test.ts src/app/state/hooks/useBackendLifecycle.test.tsx --reporter=verbose`：通过。
  - `cd backend && go test ./...`：通过。
  - `cd frontend && pnpm run test:run`：通过，262 files / 885 tests。
  - `cd frontend && pnpm run typecheck`：通过。
  - `cd frontend && pnpm run lint`：通过。
  - `cd frontend && pnpm run format:check && pnpm run preload:budget:check && pnpm run preload:boundary:check && pnpm run boundary:check && pnpm run wails-binding:check`：通过。
  - `git diff --check`：通过。
  - `cd frontend && pnpm run size:check`：仍失败于 18 个既有 size-budget baseline 文件，未包含本轮新增 preload/layout 文件。
- Size-budget 拆分方案：
  - 新增 `docs/frontend-size-budget-split-plan-2026-06-06.md`。
  - 拆分顺序：S1 wire DTO/mapper facade 拆分；S2 Evidence rules/components/page 拆分；S3 Traffic page/panels 拆分；S4 Industrial page/aux panels 拆分；S5 Stream decoder 与 MiscTools tests 拆分。
  - 每批 gate：focused tests + typecheck + lint + size:check；最终 gate 为 frontend full tests + build。
- 结论：preload 本轮健壮性复审通过；剩余 size-budget 为历史结构债，已有可执行拆分路线。

_复审人: Codex | 时间: 2026-06-06 19:40:32 +08:00_

## 2026-06-06 21:01:39 +08:00 - Codex

- 轮次：Frontend size-budget 18 项超预算拆分实施。
- 输入：按 `docs/frontend-size-budget-split-plan-2026-06-06.md` 执行 S0-S5；复核 `docs/preload-hot-loading-plan.md`、`docs/开发记录.md` 最近记录与当前代码状态。
- Hygiene：
  - 基线 `pnpm run size:check` 原为 18 个既有超预算文件失败。
  - 本轮未修改旧脏文件：`.gitignore`、`CLAUDE.md`、`README.md`、`docs/README.md`。
- S1：Wire DTO / mapper facade 拆分。
  - `captureWireDtos.ts`、`evidenceWireDtos.ts`、`trafficWireDtos.ts` 降为兼容 re-export。
  - Packet/Evidence/Traffic mapper 拆出 protocol/time/color/severity/module/metadata/conversation/protocol hierarchy helpers。
  - `securityDetectionMapper.ts` 拆为 UDP tunnel / bruteforce mapper，保持 any guard 通过。
- S2：Evidence rules / UI 拆分。
  - `evidencePanelRules.ts` 拆为 constants/value/filter/sort/count/export/label/context rules。
  - `EvidenceFilters.tsx`、`EvidenceResults.tsx`、`EvidencePanel.tsx` 拆成组合层 + focused components/hooks。
  - `evidencePanelRules.test.ts` 拆为 focused rule tests。
- S3：TrafficGraph 拆分。
  - `TrafficGraph.tsx` 抽 page state/layout。
  - `TrafficGraphPanels.tsx` 拆为 section nav、trend、topology、protocols、conversations、talkers、overview notes 与 pure model。
- S4：Industrial panels 拆分。
  - `IndustrialAuxiliaryPanels.tsx` 拆为 rule hits、control commands、protocol details、DNP3 panel。
  - 新增 DNP3 pure model/tone rules 与 focused tests；`IndustrialAnalysis.tsx` 抽 Modbus filters hook。
- S5：Stream decoder / MiscTools tests 拆分。
  - `StreamDecoderCandidatePanel.tsx`、`StreamDecoderCandidateCard.tsx` 拆为 header/metadata/apply-mode/grid/state/badges/preview/hints/fingerprints 子组件。
  - `MiscTools.test.tsx` 拆为 payload decode、payload sources、webshell metadata、confidence states focused tests，并新增 shared test harness。
- 验证：
  - `cd frontend && pnpm run size:check`：通过，0 failures。
  - `cd frontend && pnpm run typecheck`：通过。
  - `cd frontend && pnpm run lint`：通过。
  - `cd frontend && pnpm run format:check`：通过。
  - `cd frontend && pnpm exec vitest run src/app/components/StreamDecoderBatchPanel.test.tsx src/app/components/StreamDecoderToolbar.test.tsx src/app/components/StreamDecoderSettingsSections.test.tsx src/app/pages/MiscTools.test.tsx src/app/pages/MiscTools.payloadDecode.test.tsx src/app/pages/MiscTools.payloadSources.test.tsx src/app/pages/MiscTools.webshellMetadata.test.tsx src/app/pages/MiscTools.confidenceStates.test.tsx src/app/pages/MiscTools.payloadHints.test.tsx src/app/features/industrial/industrialDnp3SectionModel.test.ts`：通过，10 files / 18 tests。
  - `cd frontend && pnpm run test:run`：通过，271 files / 887 tests。
  - `cd frontend && pnpm run build`：通过。
- 结论：size-budget 旧债已消除；未提高 budget，未扩大 preload 改动面，public import facade 保持兼容。

_实施人: Codex | 时间: 2026-06-06 21:01:39 +08:00_

## 2026-06-10 18:36:44 +08:00 - Codex

- 轮次：TrafficGraph 空样本侧边栏锁死修复。
- 问题：无样本/0 包进入流量图页时，`useTrafficGraphPageState` 每次渲染传入新的 `["hunting"]`，触发 `useEvidence` effect 反复运行；空 evidence 分支反复 `setEvidence([])` 导致渲染循环，表现为侧边栏假死。
- 修复：`useEvidence` 规范化 `modules` 为稳定 key，0 包/无 capture 直接返回稳定空数组并取消请求；流量图页提取 `TRAFFIC_GRAPH_EVIDENCE_MODULES` 常量。
- 回归测试：新增空 capture 不请求 evidence、同值 modules 新数组不重复请求；保留 traffic stats 空样本短路验证。
- 验证：`cd frontend && pnpm vitest run src/app/features/evidence/useEvidence.preload.test.tsx src/app/features/traffic/useTrafficGraph.preload.test.tsx` 通过（2 files / 8 tests）；`cd frontend && pnpm run typecheck` 通过。
- 结论：无样本进入流量图页不会再触发 evidence 渲染循环或桌面 IPC/HTTP evidence 请求。

_实施人: Codex | 时间: 2026-06-10 18:36:44 +08:00_

## 2026-06-10 21:17:03 +08:00 - Codex

- 轮次：前端分析页公共工作台布局改造实施与验收。
- 范围：仅改前端布局、页面测试与开发记录；未改后端、算法、HTTP route、Wails binding、mapper 或 core types。
- 公共组件：
  - 新增 `frontend/src/app/components/analysis/AnalysisWorkbenchShell.tsx` 与 `analysisWorkbenchTypes.ts`。
  - 公共壳统一提供 `w-56` grouped side nav、section button、header、右侧 `min-h-0 flex-1 overflow-auto` 内容容器。
  - 新增 `AnalysisWorkbenchShell.test.tsx` 覆盖分组渲染、active/disabled、点击切换与滚动容器。
- 页面改造：
  - `C2Analysis` 拆为薄 wrapper + `features/c2/C2AnalysisPage.tsx`，接入 overview/family/aggregates/decrypt/evidence/report sections，并保留 CS/VShell `activeTab`。
  - `UsbAnalysis`、`AptAnalysis`、`IndustrialAnalysis`、`VehicleAnalysis` 接入公共 workbench shell；业务 panel、hook、filter/subtab/setup 状态保持原 feature 逻辑。
  - APT actor 与固定 section 使用公共 grouped side nav；无 actor 时保持稳定 empty state。
  - 本轮未从 TrafficGraph/MISC 私有布局组件导入公共壳，只参考视觉结构。
- 测试补强：
  - 更新 C2/USB/APT/Industrial 页面测试以适配 section 切换。
  - 新增 `VehicleAnalysis.test.tsx` 覆盖 setup/details/report 切换与 DBC/UDS 状态保留。
  - 新增 C2 helpers/fixtures，并新增 `C2Analysis.testState.ts` 复用测试 seed，解决 C2 focused tests size budget 风险。
- 验证：
  - `cd frontend && pnpm exec tsc --noEmit --noUnusedLocals --noUnusedParameters`：通过。
  - `cd frontend && pnpm exec vitest run src/app/components/analysis/AnalysisWorkbenchShell.test.tsx src/app/pages/C2Analysis.test.tsx src/app/pages/C2Analysis.decrypt.test.tsx src/app/pages/C2Analysis.candidates.test.tsx src/app/pages/C2Analysis.vshell.test.tsx`：通过，5 files / 16 tests。
  - `cd frontend && pnpm run boundary:check`：通过。
  - `cd frontend && pnpm run wails-binding:check`：通过。
  - `cd frontend && pnpm run desktop-generic-ipc:check`：通过。
  - `cd frontend && pnpm run ci`：通过，273 files / 897 tests，并完成 production build。
- 结论：第一批 C2/USB/APT/Industrial/Vehicle 分析页已统一到公共桌面工作台布局；主线仍为 Wails typed IPC，未新增 generic IPC 缺口。

_实施人: Codex | 时间: 2026-06-10 21:17:03 +08:00_

## 2026-06-11 15:09:13 +08:00 - Codex

- 轮次：后端测试覆盖面补齐到 85%+。
- 范围：仅新增后端测试，不改生产代码、API shape、Wails binding、HTTP route、前端或算法行为；测试均使用内存样本、函数 stub 或纯逻辑输入，不依赖真实 tshark/ffmpeg/yara/python/vosk 外部二进制。
- 新增覆盖面：
  - `backend/internal/engine/tool_c2_more_85_test.go`：覆盖 C2 APT enrichment、CS Host/URI 聚合、CS DNS 聚合、VShell stream 聚合、HTTP observation promotion、score factor 分类与排序/limit/clamp 分支。
  - `backend/internal/engine/stream_payload_inspector_more_85_test.go`：覆盖 AntSword chr-chain、hex cipher、Godzilla hex cipher、hex payload、空 payload、嵌入 base64/hex token、hex-wrapped HTTP form、JSON invalid/limit/order 分支。
  - `backend/internal/engine/service_streams_more_85_test.go`：覆盖 HTTP stream file fallback/error、Raw stream cache/index/cancel/file-error、RawStreamPage fallback、empty patch delegation、override/meta helper 分支。
- 覆盖率结果：
  - 起始精算基线约 `84.37%`，距离 85% 约 138 条有效语句。
  - 最终精算 `18734 / 22014` statements，后端总覆盖率 `85.1%`，超过 85% 约 22 条有效语句。
  - `internal/engine` 包覆盖率提升到 `85.9%`。
- 验证：
  - `cd backend && go test ./internal/engine -run "TestC2APTEnrichmentCoversTransportDeliveryAndFamilyBranches|TestBuildCSHostURIAggregatesCoversScoreAndFallbackBranches|TestBuildCSDNSAggregatesCoversTypeRatioAndClampBranches|TestBuildVShellStreamAggregatesCoversSkipSortAndSummaryBranches|TestPromoteCSHTTPObservationsCoversStableHighVolumeBranches|TestClassifyScoreFactorCoversPositiveNegativeAndUnknownTags" -count=1 -v`：通过。
  - `cd backend && go test ./internal/engine -run "TestFingerprintPayloadCandidateCoversChrAndHexBranches|TestInspectStreamPayloadCoversFallbackAndEmbeddedTokenBranches|TestCollectJSONCandidatesCoversInvalidLimitsAndOrdering" -count=1 -v`：通过。
  - `cd backend && go test ./internal/engine -run "TestHTTPStreamFileFallbackAndErrorBranches|TestRawStreamCacheIndexCancelAndFileErrorBranches|TestRawStreamPageFallbackAndEmptyPatchBranches|TestStreamMetaAndOverrideHelperBranches" -count=1 -v`：通过。
  - `cd backend && go test ./...`：通过。
  - `cd backend && go test ./... -covermode=count -coverprofile="$env:TEMP\gshark-backend-cover-goal-audit.out"`：通过。
  - `cd backend && go tool cover -func="$env:TEMP\gshark-backend-cover-goal-audit.out"`：total `85.1%`。
  - `cd backend && gofmt -l internal\engine\tool_c2_more_85_test.go internal\engine\stream_payload_inspector_more_85_test.go internal\engine\service_streams_more_85_test.go`：无输出。
- 结论：后端总测试覆盖率已达到 85%+，并由全量测试、coverprofile 精算和 go tool cover total 共同验证。

_实施人: Codex | 时间: 2026-06-11 15:09:13 +08:00_

## 2026-06-12 19:23:11 +08:00 - Codex

- 轮次：后端 Engine 瘦身与前端 CI 拆分优化。
- 范围：只处理 `engine` 单体大包与前端 `pnpm run ci` 膨胀；未做品牌迁移，未改 HTTP/Wails/DTO 契约或算法行为。
- 后端改动：
  - 新增 `backend/internal/engine/payloadinspect/`，将 WebShell payload inspection 纯逻辑迁出根 `engine`；根包保留 `InspectStreamPayload` façade，调用方不变。
  - 新增 `backend/internal/engine/shared_helpers.go` 承接仍被根包多领域复用的轻量 helper，避免子包反向依赖根包。
  - 扩展 `backend/internal/architecture/boundary_test.go`：要求 engine 根文件登记领域归属、新增大文件必须显式例外、纯逻辑子包不得依赖根 engine/transport/tshark/HTTP/exec。
- 前端/CI 改动：
  - `frontend/package.json` 将顶层 `ci` 拆为 `ci:quality`、`ci:boundaries`、`ci:desktop`、`ci:test-build` 四组，原单项脚本全部保留。
  - `.github/workflows/ci.yml` 将 frontend job 拆为四个可观测 step；`scripts/check-all.ps1` 同步调用相同分组。
  - `docs/project-development-guide.md`、`docs/project-design-and-constraints.md` 同步记录 CI 分组与 engine 子包约束。
- 验证：
  - `cd backend && go test ./internal/engine ./internal/engine/payloadinspect -run "TestInspectStreamPayload|TestFingerprintPayloadCandidate|TestCollectJSONCandidates|TestDecodeStreamPayloadAuto" -count=1`：通过。
  - `cd backend && go test ./internal/architecture -run TestBackendArchitectureBoundaries -count=1 -v`：通过。
  - `cd backend && go test ./...`：通过。
  - `cd frontend && pnpm run ci:quality`：通过。
  - `cd frontend && pnpm run ci:boundaries`：通过。
  - `cd frontend && pnpm run ci:desktop`：通过。
  - `cd frontend && pnpm run ci:test-build`：通过，274 files / 901 tests，并完成 production build。
  - `cd frontend && pnpm run ci`：通过，四组聚合链路可用。
  - `powershell -ExecutionPolicy Bypass -File .\scripts\check-all.ps1`：通过，包含 desktop dev/production tag 测试、backend boundary/focused/full tests、frontend 四分组 CI 与 Wails build/assets check。
  - `git diff --check`：通过。
- 结论：第一批 engine 纯逻辑抽离完成，后续新增根包文件与大文件增长会被架构门禁拦截；前端 CI 失败域从单条长命令拆为四个明确阶段。

_实施人: Codex | 时间: 2026-06-12 19:23:11 +08:00_

## 2026-06-12 21:45:00 +08:00 - Codex

- 轮次：全量一期治理修复。
- 范围：围绕 13 项高/中/低问题做一期落地；未做品牌迁移，未改 HTTP route、Wails binding、OpenAPI wire shape 或核心算法输出语义。
- 后端治理：
  - `engine.Service` 保持对外 façade，但内部状态改为显式 controller 字段组合，按 capture/filter/stream/analysis/object/media/YARA-runtime/MCP/playbook/saved-search/hypothesis 分组。
  - 架构门禁新增 engine root ownership、大文件例外、纯逻辑子包依赖、Service 显式 controller、controller 单 state group 检查。
  - WebShell payload inspection 纯逻辑迁入 `backend/internal/engine/payloadinspect/`，根包保留兼容 façade。
  - `tool_*.go` 共用抓包/packet store 前置检查抽为 `packetsForToolAnalysis()`，保留业务算法不变。
  - YARA scan 等待改为真实 completion signal；timeout/cancel/runner error 保留 `%w` cause，并补并发等待、cancel、timeout 测试。
- 前端治理：
  - 新增 `analysisResourceCache` 与 `hasUsableCapturePath`，C2/APT/Industrial/Vehicle/Media/USB/Traffic/Evidence hooks 统一空样本 guard、cache key、inflight dedupe 和 force refresh 语义。
  - 新增 `KnownOrUnknown<T>`，核心类型开放 `| string` 收敛到兼容类型；wire DTO 宽 `Record<string, unknown>` 改为例外清单治理。
  - `desktopTypedBridgeRules.ts` 去除裸 `as any`，规则相关 IPC payload 改 typed normalizer 并补测试。
  - `useSentinelProviderBody.ts` 从 542 行降到 451 行；context value assembly 拆到 `useSentinelDomainValues` / `useSentinelContextValues`，legacy context shape 保持不变。
  - `pnpm run ci` 已分为 quality/boundaries/desktop/test-build 四组，type governance 纳入 boundaries。
- 低严重度治理：
  - `.gitignore` 补 root `package-lock.json` 与 `.mimocode/`；误跟踪的 root `package-lock.json` 已从索引移除。
  - 新增 ignored tracked files 检查并接入本地 `check-all.ps1` 与 CI。
  - 新增 `CONTRIBUTING.md`、`CHANGELOG.md`、`docs/full-governance-phase1-register.md`，并更新 README / docs 入口。
- 验证：
  - `cd backend && gofmt -l .`：通过。
  - `cd backend && go test ./internal/architecture -run TestBackendArchitectureBoundaries -count=1 -v`：通过。
  - `cd backend && go test ./...`：通过。
  - `cd frontend && pnpm run type-governance:check`：通过。
  - `cd frontend && pnpm run typecheck`：通过。
  - `cd frontend && pnpm run size:check`：通过。
  - `cd frontend && pnpm exec vitest run src/app/state/useSentinelProviderBody.test.ts src/app/core/analysisResourceCache.test.ts src/app/integrations/desktopTypedBridgeRules.test.ts scripts/check-type-governance.test.mjs scripts/ci-script-coverage.test.mjs --reporter=verbose`：通过，5 files / 15 tests。
  - `powershell -ExecutionPolicy Bypass -File .\scripts\check-ignored-tracked-files.ps1`：通过。
  - `git diff --check`：通过。
- 结论：一期治理已形成“后端 Service/engine 边界 + 前端 type/cache/context/filePath guard + ignored artifact gate + 文档登记”的可持续门禁。剩余可继续按领域逐步迁移 engine 纯逻辑和收敛既有 wire DTO 例外。

_实施人: Codex | 时间: 2026-06-12 21:45:00 +08:00_
